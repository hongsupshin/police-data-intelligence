# Minimal dependencies for CI testing
# This avoids installing heavy packages (torch, transformers, etc.) in CI

# Core dependencies used by tested code
pandas>=2.2.0
psycopg2-binary>=2.9.9
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dateutil>=2.9.0
python-dotenv>=1.0.0
tavily-python>=0.5.0

# Testing dependencies
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# Linting
ruff>=0.6.0
